#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		int u,v,V,a,s;
		cin>>u>>V>>a>>s;
		
		if((pow(u,2)-(2*a*s))<=pow(V,2))
		  cout<<"Yes"<<endl;
		else
		   cout<<"No"<<endl;
	
	
}
}
